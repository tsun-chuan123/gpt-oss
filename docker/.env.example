# GPT-OSS Vision Model Docker 環境變數配置
# 複製此檔案為 .env 並根據需求修改

# GPU 設定
CUDA_VISIBLE_DEVICES=0  # 使用的 GPU 編號,多張可設為 0,1,2,3

# Hugging Face Token (下載需要認證的模型時使用)
# HF_TOKEN=your_huggingface_token_here

# Weights & Biases 配置 (訓練追蹤)
# WANDB_API_KEY=your_wandb_api_key_here
WANDB_PROJECT=gpt-oss-vision

# 服務端口
JUPYTER_PORT=8888       # Jupyter Notebook
TENSORBOARD_PORT=6006   # TensorBoard

# 資源限制
SHM_SIZE=16gb          # 共享記憶體大小 (訓練時建議 16gb 以上)
# MEMORY_LIMIT=64g     # 記憶體限制
# CPU_LIMIT=16         # CPU 核心數限制

# 快取路徑 (可選,預設為 docker volume)
# CACHE_PATH=./cache

# 本地模型路徑 (如果您有本地模型檔案)
# LOCAL_MODEL_PATH=./models
